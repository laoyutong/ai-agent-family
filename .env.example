# DeepSeek API（与 OpenAI SDK 兼容，密钥在控制台创建）
# 复制为仓库根目录 `.env` 后填写，勿提交 `.env`
DEEPSEEK_API_KEY=

# 可选覆盖（默认见 apps/chatbot-memory）
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-chat
