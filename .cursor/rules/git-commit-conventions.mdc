---
description: 本仓库 git 提交说明格式（中文、多段正文换行）
alwaysApply: true
---

# Git 提交说明

- **标题**：`type(scope): 简短概括`，`type` 沿用 Conventional Commits（如 `feat`、`fix`、`docs`、`refactor`）；`scope` 可选（如受影响的包/目录）。标题用中文写清「做了什么」，必要时可夹英文标识。
- **空一行**：标题与正文之间必须有一行空行。
- **正文（多条内容）**：用 **换行分条** 更清晰时，每条占一行，行前可用 `- `；若仍为一段连续说明，可用中文分号 `；` 隔句，避免一锅逗号到底。
- **语气**：完整句子，说明「改了什么、为何相关」，不写无意义的占位句。
- **工具**：若默认 `git commit` 报「未知选项 trailer」，在本机可用 Homebrew 的 git：`/opt/homebrew/bin/git commit`。

示例：

```
feat(aura-code): 会话区合并模型与工具块并收紧流式展示

- 按轮次交错渲染正文与工具步骤
- 流式中空段落不占行；工具条去掉前置装饰符
- 用户与助手消息的换行规则对齐
```
